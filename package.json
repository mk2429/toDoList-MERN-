{
  "name": "deployedtodomern",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"node backend/dist/index.js\" \"cd frontend && npm start\"",
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
